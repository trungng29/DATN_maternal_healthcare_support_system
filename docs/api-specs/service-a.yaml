# ============================================
# Service A — OpenAPI 3.0 Specification
# Docs: https://swagger.io/specification/
# ============================================
openapi: 3.0.3
info:
  title: Service A API
  description: |
    API specification for Service A.
    Replace with your actual service name and description.
  version: 1.0.0

servers:
  - url: http://localhost:5001
    description: Local development

paths:
  /health:
    get:
      summary: Health check
      operationId: healthCheck
      responses:
        '200':
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: ok

  # Add your endpoints below
  # /items:
  #   get:
  #     summary: List all items
  #     operationId: listItems
  #     responses:
  #       '200':
  #         description: A list of items
  #         content:
  #           application/json:
  #             schema:
  #               type: array
  #               items:
  #                 $ref: '#/components/schemas/Item'
  #   post:
  #     summary: Create a new item
  #     operationId: createItem
  #     requestBody:
  #       required: true
  #       content:
  #         application/json:
  #           schema:
  #             $ref: '#/components/schemas/ItemCreate'
  #     responses:
  #       '201':
  #         description: Item created

components:
  schemas: {}
    # Item:
    #   type: object
    #   properties:
    #     id:
    #       type: string
    #       format: uuid
    #     name:
    #       type: string
    #     createdAt:
    #       type: string
    #       format: date-time
    # ItemCreate:
    #   type: object
    #   required:
    #     - name
    #   properties:
    #     name:
    #       type: string